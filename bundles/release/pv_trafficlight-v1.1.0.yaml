# Bundle发布版本 - 正式发布快照
# 目的：生产环境的严格版本控制和长期稳定性

meta:
  bundle_name: pv_trafficlight
  bundle_version: "v1.1.0"             # 语义化版本
  bundle_type: release                 # 发布版本，长期支持
  created_at: "2025-06-20T14:40:00Z"
  created_by: bundle_generator
  source_consumer: pv_trafficlight
  consumer_version: "1.1.0"
  description: "感知红绿灯系统 v1.1.0 正式发布版本"

# 来源追溯
source_consumer: consumers/pv_trafficlight/v1.1.0.yaml
snapshot_time: "2025-06-20T14:40:00Z"

# 硬约束通道配置
channels:
  - channel: image_original
    version: "1.2.0"
    locked_at: "2025-06-20T14:40:00Z"
    required: true
    source_constraint: "1.2.0"
    spec_file: "channels/image_original/spec-1.2.0.yaml"
    
  - channel: object_array_fusion_infer
    version: "1.2.0"
    locked_at: "2025-06-20T14:40:00Z"
    required: true
    source_constraint: ">=1.2.0"
    spec_file: "channels/object_array_fusion_infer/spec-1.2.0.yaml"
    
  - channel: utils_slam
    version: "1.1.0"
    locked_at: "2025-06-20T14:40:00Z"
    required: true
    source_constraint: "^1.1.0"
    spec_file: "channels/utils_slam/spec-1.1.0.yaml"

# 发布版本特性
release_config:
  stability_level: "stable"            # 稳定性等级
  support_until: "2026-06-20"          # 支持截止时间
  backward_compatible: true            # 向后兼容
  quality_gate_passed: true            # 质量门控通过
  approval_status: "approved"          # 审批状态

# 完整性验证
integrity_hash: "abc123def456789a"

# 质量指标
quality_metrics:
  data_completeness: 0.99
  validation_coverage: 1.0
  test_pass_rate: 0.98
  performance_baseline: "met"

# 使用说明
usage:
  description: "感知红绿灯生产环境数据版本"
  target_scenarios: ["production_deployment", "regression_testing"]
  load_command: "dataspec load --bundle bundles/release/pv_trafficlight-v1.1.0.yaml"
  
# 变更说明
changelog:
  - version: "v1.1.0"
    changes: ["升级object_array_fusion_infer至1.2.0", "新增utils_slam高精度定位支持"]
    breaking_changes: [] 